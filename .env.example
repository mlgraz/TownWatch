# Environment Configuration for TownWatch
# Copy this file based on your environment:
#   - Development: Uses .env.development (AsyncStorage - local only)
#   - Testing:     Uses .env.test (Supabase - cloud)
#   - Production:  Uses .env.production (Supabase - cloud)

# Environment (development, test, production)
# Set the appropriate value per environment file
NODE_ENV=

# Supabase Configuration (only needed for test/production)
# Get these values from: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Copy this template to the files required by the app:
#   cp .env.example .env.development
#   cp .env.example .env.test
#   cp .env.example .env.production
# Then fill in the correct values for each environment locally.*** End Patch
